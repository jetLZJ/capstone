import ReferenceClasses from './ReferenceClasses';

// This file just ensures the ReferenceClasses component gets included in the bundle
// It's never actually used or rendered

export const _ensure_tailwind_classes_bundled = ReferenceClasses;